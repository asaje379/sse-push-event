{
  "name": "@asaje/sse-push-event",
  "version": "1.0.9",
  "main": "./dist/main.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.18.11",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "build": "tsc && esbuild src/*.ts --outdir=dist --minify --packages=external"
  },
  "files": [
    "dist"
  ],
  "types": "./dist/main.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/asaje379/sse-push-event.git"
  }
}
