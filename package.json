{
  "name": "@asaje/sse-push-event",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.18.11",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "build": "tsc && esbuild src/*.ts --outdir=dist --minify"
  }
}
